apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
data:
  # Override backend URL for local Kind cluster
  # Frontend will call /api/proxy which is handled by Next.js API route
  # The API route then forwards requests to the backend service
  BACKEND_URL: "/api/proxy"
